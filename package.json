{
  "name": "node-gts-jest-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/src"
  ],
  "license": "MIT",
  "keywords": [],
  "scripts": {
    "start": "npm run compile:prod && node dist/index.js",
    "test": "jest --coverage",
    "debug": "node --nolazy --inspect-brk=9339 dist/index.js",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "compile:prod": "tsc -p tsconfig.prod.json",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "devDependencies": {
    "@types/jest": "^24.0.22",
    "@types/node": "^10.0.3",
    "gts": "^1.1.0",
    "jest": "24.9.0",
    "ts-jest": "^24.1.0",
    "typescript": "^3.7.2"
  },
  "dependencies": {}
}
